{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Start Clinical Ops API",
			"type": "shell",
			"command": "$env:PYTHONPATH=\".\"; python -m uvicorn api.main:app --host 127.0.0.1 --port 8000 --reload"
		}
	]
}